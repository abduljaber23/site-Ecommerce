{% extends 'base.html.twig' %} {% block title %}Hello AdminCategoryController!{%
endblock %} {% block body %}
<div class="ti">
  <div class="titre">
    <h3>
      {{ category.label }}
    </h3>
  </div>
  <div class="link">
    <a href="{{ path('edit_cate', { id: category.id }) }}"
      ><i class="fa-solid fa-pen-to-square blu"></i
    ></a>
    <a href="{{ path('remove_cate', { id: category.id }) }}"
      ><i class="fa-solid fa-trash" id="red" ></i
    ></a>
  </div>
</div>

{% endblock %}
